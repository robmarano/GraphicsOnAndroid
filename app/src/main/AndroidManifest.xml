<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thehackerati.android.apps.graphicsonandroid">

    <application android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/AppTheme">
        <activity
            android:name=".DrawBitmapActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="DrawGradientActivity" >
        </activity>
        <activity android:name="DrawShapeActivity" >
        </activity>
        <activity android:name="DrawTextActivity" >
        </activity>
        <activity android:name="DrawCustomFontActivity" >
        </activity>    </application>

</manifest>
